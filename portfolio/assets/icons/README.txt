This folder is for icon assets (e.g. a real favicon, social icons), if you
add any. The site currently uses a small inline SVG favicon defined directly
in each HTML file's <head>, so nothing here is required for the site to work.
